import { PlaceholderBody } from '../../components/PlaceholderBody';

export function AdminDashboardPage() {
  return (
    <PlaceholderBody
      title="Admin Dashboard"
      description="KPI cards, user & store management will live here."
    />
  );
}
